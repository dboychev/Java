The class started with an empty array with 10 cells. The size of the array always increases by multiplying by 2.
10 -> 20 -> 40 -> 80 -> 160 -> 320. Now the array is big enough to contain 280 elements. 

320